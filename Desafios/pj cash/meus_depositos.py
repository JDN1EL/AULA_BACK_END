#numero cliemnte
#numero conta conta
#valor

cliente= {'nc':input('Seu numero de cliente: '),
    'nconta':float(input('Ok, agora seu número de conta: ')),}

depositar = {float(input('''AQUI NA PJ VOCÊ NÃO TEM PREJU!!:
QUANTO QUER ADICIONAR? '''))}

while depositar != 0:
#if depositar != 0:
    depositado = print('Muito bem, deposito concluido com sucesso')
    break
if depositar == 0:
    print('Erro valor indefinido, por motivos de segurança vamos saiir, tente novamente mauis tarde')


'''from cliente import nca
if cliente != nca:
    verificacao = print('Antes de tudo, necessário alguns dados que não se encontam no sistema 1 para fazer 2 para sair')
    if verificacao == 1:
        from cliente import dados
    if verificacao == 2:
        exit'''