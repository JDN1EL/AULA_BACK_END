from ast import Str


um = 1
dois = 2
inicio = int(input('Olá! bem vindo(a)!\nPara seu cadastro pressione 1 se não, pressione 2\n'))
from cliente import dados, submenu
if inicio == um:
    while True:
        dados
        if submenu == 1:
            from conta_corrente import coleta_de_dados_um
        
        
if inicio == dois:
    exit
#teste